import styled from 'styled-components'

const Footer = styled.footer`
    display: flex;
    align-items: center;
    justify-content: center;

    width: 100%;
    height: 100px;

    background: #52ceff;
    box-shadow: 1px 1px 5px 5px #00000013;

    color: #ffffff;
    font-family: Arial, serif;
    font-size: 16px;
    text-shadow: 0 0 5px #0000007d;

    position: absolute;
    bottom: 0;
    left: 0;
`

const Styled = {
    Footer
}

export default Styled
